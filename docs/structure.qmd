# Workflow Structure

To be introduced on day #2

<!-- Introducing the lib and bin directories, as well as templates for larger processes. What should be included in a Docker file. Nextflow schema json. Standard directory structures for modules. Custom Groovy classes - leveraging custom Groovy.

Using groovy functions that load json. Use Harshil’s Fastp example for. Workflow helper class.

Functions local in file
Functions imported
Functions in separate class -->
